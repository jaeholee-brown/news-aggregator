# ==================== NEWS AGGREGATOR CONFIGURATION ====================

# Metaculus API (optional - for authenticated requests)
METACULUS_TOKEN=your_metaculus_token

# News Aggregation
EXA_API_KEY=your_exa_api_key
FIRECRAWL_API_KEY=your_firecrawl_api_key  # Optional, for enhanced content extraction

# LLM (OpenAI) - for change detection
OPENAI_API_KEY=your_openai_api_key

# Email Notifications (Gmail SMTP)
GMAIL_USER=your_email@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx  # 16-digit app password from Google Account

# Email recipients (comma-separated)
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com

# Questions to track (comma-separated)
QUESTION_IDS=
SERIES_IDS=
SERIES_SLUGS=  # Alternative to SERIES_IDS - use slugs like "bridgewater" instead of numeric IDs

# Model Configuration (optional - defaults shown)
# CHANGE_DETECTION_MODEL=gpt-5-mini

# Data storage directory (optional - defaults to "data")
# DATA_DIR=data
